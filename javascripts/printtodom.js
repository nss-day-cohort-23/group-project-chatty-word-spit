"use strict";
require('./dominteractions');

let createMsgDiv = (message) => {
    console.log(message);
    // addDeleteBtn(varNameOfNewDiv);
};

let addDeleteBtn = (div) => {
    console.log(div);
};

module.exports = createMsgDiv;